# Controls the visibility of the on-screen controller (OSC).
# "always" means the OSC is always visible during playback.
visibility=always

# Display playback timecodes with milliseconds precision (e.g. HH:MM:SS.mmm)
# instead of whole seconds only (e.g. HH:MM:SS).
timems=yes
